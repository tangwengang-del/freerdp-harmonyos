# 两步检测优化 - 2026-01-05

## 📋 重大优化内容

### ✅ 触发时机变更：从延迟500ms → 鼠标松开立即检测

**修改前：**
- 指针静止500ms后触发检测
- 松开鼠标按钮后延迟100ms再检测

**修改后：**
- ✅ 鼠标松开左键或中键时，**立即检测**（无延迟）
- ✅ 响应更快，用户体验更好

---

### ✅ 两步检测策略：快速预扫描 + 详细判定

#### 第一步：快速预扫描
- **扫描范围**：Y±4像素（9行）× 左右5px（10px）
- **扫描像素**：90像素
- **检测目标**：是否有红色或绿色像素
- **目的**：快速确定是否有虚实线候选，保证**最远4像素能检测到**

#### 第二步：详细判定
- **触发条件**：仅在第一步发现红绿线时执行
- **扫描范围**：以发现的红绿线为中心，Y±2像素（5行）× 左右100px（200px）
- **扫描像素**：1000像素
- **检测目标**：判定是否符合虚实线模式（线段数、断开数、最长线段等）
- **目的**：精准判定，避免误判

---

## 🔧 具体修改

### 修改1：去掉延迟，立即检测（TouchPointerView.java 第552-555行）

**修改前：**
```java
if (wasLeftDragging || wasCenterMoving)
{
    postDelayed(new Runnable() {
        @Override
        public void run()
        {
            detectDashedLine();
        }
    }, 100);  // 延迟100ms
}
```

**修改后：**
```java
// 松开左键拖拽或中键移动后，立即检测虚实线（不延迟）
if (wasLeftDragging || wasCenterMoving)
{
    detectDashedLine();  // 立即执行
}
```

---

### 修改2：新增快速预扫描方法

```java
// 第一步：快速预扫描 - 检查指定行的小范围内是否有红绿像素
// 用于Y±4像素（9行）× 左右5px（10px）的快速检测
private boolean hasRedGreenPixelsInRange(Bitmap bitmap, int y, int centerX, int range)
{
    int leftBound = Math.max(0, centerX - range);
    int rightBound = Math.min(bitmap.getWidth(), centerX + range);
    int width = rightBound - leftBound;
    
    if (width <= 0) return false;
    
    // 读取像素
    int[] pixels = new int[width];
    try
    {
        bitmap.getPixels(pixels, 0, width, leftBound, y, width, 1);
    }
    catch (Exception e)
    {
        return false;
    }
    
    // 检查是否有红色或绿色像素
    for (int pixel : pixels)
    {
        if (isTargetColorPixel(pixel))
        {
            return true;  // 发现红绿线，立即返回
        }
    }
    
    return false;
}
```

---

### 修改3：重写检测主逻辑，实现两步检测

```java
// 在后台线程执行两步检测
new Thread(new Runnable() {
    @Override
    public void run()
    {
        // ========== 第一步：快速预扫描（Y±4像素，左右各5px） ==========
        int quickScanRange = 5;  // 左右各5px（共10px）
        boolean hasRedGreenLine = false;
        int candidateY = -1;
        int minDistanceStep1 = 999;
        
        // 扫描9行（Y-4到Y+4）
        for (int dy = -4; dy <= 4; dy++)
        {
            int y = coord.y + dy;
            if (y < 0 || y >= bitmap.getHeight()) continue;
            
            // 只扫描指针左右各5px（共10px）
            if (hasRedGreenPixelsInRange(bitmap, y, coord.x, quickScanRange))
            {
                hasRedGreenLine = true;
                int distance = Math.abs(dy);
                if (distance < minDistanceStep1)
                {
                    minDistanceStep1 = distance;
                    candidateY = y;
                }
            }
        }
        
        if (!hasRedGreenLine)
        {
            // 未找到红绿线，清除旧状态
            return;
        }
        
        // ========== 第二步：详细判定（Y±2像素，左右100px） ==========
        int foundY = -1;
        int minDistanceStep2 = 999;
        
        // 以candidateY为中心，扫描Y±2像素（5行）
        for (int dy = -2; dy <= 2; dy++)
        {
            int y = candidateY + dy;
            if (y < 0 || y >= bitmap.getHeight()) continue;
            
            // 详细检测：全范围（左右100px），判定是否是虚实线
            if (isDashedLineAt(bitmap, y, coord.x))
            {
                int distanceFromPointer = Math.abs(y - coord.y);
                if (distanceFromPointer < minDistanceStep2)
                {
                    minDistanceStep2 = distanceFromPointer;
                    foundY = y;
                }
            }
        }
        
        // 找到虚实线，自动移动
        if (foundY != -1)
        {
            final int targetY = foundY;
            post(new Runnable() {
                @Override
                public void run()
                {
                    moveToTarget(coord.x, targetY);
                }
            });
        }
    }
}).start();
```

---

## 📊 性能对比

### 扫描像素数对比

| 方案 | 第一步 | 第二步 | 总计（最坏） | 总计（最好） |
|------|--------|--------|--------------|--------------|
| **旧方案** | - | 200px×5行=1000px | 1000px | 1000px |
| **新方案** | 10px×9行=90px | 200px×5行=1000px | 1090px | 90px |

### 实际场景分析

| 场景 | 旧方案 | 新方案 | 提升 |
|------|--------|--------|------|
| **无虚实线区域** | 1000px | 90px | **快11倍** |
| **有虚实线** | 1000px | 1090px | 略慢9% |
| **有红绿线但非虚实线** | 1000px | 1090px | 略慢9% |

### 响应速度对比

| 项目 | 旧方案 | 新方案 | 提升 |
|------|--------|--------|------|
| **触发延迟** | 静止500ms + 延迟100ms | 松开立即执行 | **快600ms** |
| **检测速度（无虚实线）** | 1000px | 90px | **快11倍** |
| **检测速度（有虚实线）** | 1000px | 1090px | 略慢9% |

---

## 🎯 关键优势

### 1. **保证最远4像素能检测到**
- ✅ 第一步扫描9行（Y-4到Y+4），覆盖±4像素范围
- ✅ 满足"最远4像素就能移动对准"的核心需求

### 2. **响应速度提升**
- ✅ 去掉100ms延迟，鼠标松开立即检测
- ✅ 用户体验更好，反应更灵敏

### 3. **大多数场景性能优化**
- ✅ 无虚实线区域（最常见）：只扫描90像素，快11倍
- ✅ 有虚实线区域：扫描1090像素，略慢9%但可接受
- ✅ 整体性能提升，尤其在无虚实线区域

### 4. **精准判定**
- ✅ 第一步快速排除无关区域
- ✅ 第二步详细判定虚实线模式
- ✅ 先找红绿线，再判定模式，逻辑清晰

### 5. **智能分步**
- ✅ 第一步：覆盖范围大（±4像素），像素少（10px宽）
- ✅ 第二步：仅在必要时执行，范围小（±2像素），像素多（200px宽）
- ✅ 平衡了检测范围和性能

---

## 📐 检测流程图

```
松开鼠标左键/中键
    ↓
立即执行检测（无延迟）
    ↓
【第一步：快速预扫描】
扫描：Y±4像素（9行）× 左右5px（10px）= 90像素
检测：是否有红/绿像素？
    ↓ NO → 结束（未检测到）90像素，快11倍 ✅
    ↓ YES
    ↓
记录最近的红绿线Y坐标
    ↓
【第二步：详细判定】
扫描：以红绿线为中心，Y±2像素（5行）× 左右100px（200px）= 1000像素
检测：是否符合虚实线模式？
  - 线段数 ≥ 2？
  - 断开数 ≥ 2？
  - 最长线段 ≤ 30px？
    ↓ NO → 结束（不是虚实线）1090像素
    ↓ YES
    ↓
✅ 自动移动指针到虚实线
✅ 指针变红色
✅ 可拖动虚实线
```

---

## 🎯 设计理念

### 为什么是两步检测？

#### 第一步：广度优先（覆盖范围）
- **目标**：不漏掉任何可能的虚实线
- **策略**：扫描范围大（±4像素，9行），但宽度小（10px）
- **效果**：快速确定"有没有"，保证覆盖

#### 第二步：深度优先（精准判定）
- **目标**：准确判定是否是虚实线
- **策略**：扫描范围小（±2像素，5行），但宽度大（200px）
- **效果**：详细分析"是不是"，避免误判

### 为什么第一步只扫描10px宽？

1. **红绿线通常在指针附近**：
   - 用户移动指针时，通常会让指针接近目标线
   - 10px范围（±5px）足够检测到大多数情况

2. **极快的响应速度**：
   - 90像素（10px×9行）扫描极快
   - 大多数无虚实线的场景立即结束

3. **平衡性能和覆盖**：
   - 如果太宽（如±20px），第一步就变慢了
   - 如果太窄（如±2px），可能漏掉偏离中心的线
   - ±5px是最佳平衡点

### 为什么第二步用±2像素？

1. **精准定位**：
   - 第一步已经找到红绿线的大致位置（candidateY）
   - 在candidateY±2像素范围内精确判定
   - 5行范围足够覆盖该虚实线

2. **避免误判**：
   - 如果用±4像素（9行），可能包含相邻的其他线条
   - ±2像素（5行）更聚焦，减少干扰

3. **性能优化**：
   - 第二步扫描200px宽，像素数较多
   - 用5行而不是9行，减少44%的像素扫描

---

## 🧪 测试场景

### 应该正常检测的情况：
✅ 虚实线在指针上方1-4像素
✅ 虚实线在指针下方1-4像素
✅ 虚实线正好在指针位置
✅ 虚实线略微偏离指针中心（±5px内）
✅ 虚实线中间有竖线（排除区域）

### 应该快速排除的情况：
❌ 无虚实线区域 → 第一步90像素后立即结束 ✅
❌ 虚实线在指针上方5像素以上（超出±4范围）
❌ 虚实线在指针下方5像素以上（超出±4范围）
✅ 有红绿线但不符合虚实线模式 → 第二步排除

---

## ✅ 编译状态

- ✅ 编译通过
- ✅ 无linter错误
- ✅ 逻辑完整
- ✅ DEBUG日志完善

---

## 📝 修改位置总结

**文件：** `TouchPointerView.java`

**修改行号：**
1. 第552-555行：去掉100ms延迟，立即执行检测
2. 第735-764行：新增 `hasRedGreenPixelsInRange()` 方法（第一步）
3. 第766-778行：保持 `isDashedLineAt()` 方法（第二步）
4. 第680-782行：重写检测主逻辑，实现两步检测

---

## 🎯 核心要点总结

1. **触发时机**：鼠标松开立即检测（无延迟）✅
2. **覆盖范围**：第一步Y±4像素，保证最远4像素能检测到 ✅
3. **性能优化**：无虚实线场景快11倍（90像素 vs 1000像素）✅
4. **精准判定**：第二步Y±2像素详细判定，避免误判 ✅
5. **用户体验**：响应更快，反应更灵敏 ✅

---

**修改日期：** 2026-01-05  
**修改原因：** 优化检测性能，保证最远4像素能检测到  
**核心需求：** "最远4像素就能移动对准虚实线"  
**预期效果：** 响应更快，性能更好，覆盖更全
